---
'@flexkit/studio': patch
---

Add an entity selector to global search so queries can be limited to one collection, hide the empty state while results are loading, and reset the filter when the project or schema changes.
