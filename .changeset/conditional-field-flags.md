---
'@flexkit/studio': patch
'@flexkit/cli': patch
---

Rename attribute flags to `hidden`, `readOnly`, `unique`, and `searchable`, and support conditional `hidden` / `readOnly` callbacks in Studio forms.
