{
  "extends": ["//"],
  "pipeline": {
    "build": {
      "dotEnv": [".env.local", ".env"],
      "env": []
    },
    "dev": {
      "dotEnv": [".env.local", ".env"],
      "env": ["DEBUG"]
    }
  }
}
